{
  "name": "backend",
  "private": true,
  "scripts": {
    "setup": "uv sync",
    "dev": "uv run uvicorn --port 7935 --reload main:app",
    "start": "uv run uvicorn --port 7936 main:app"
  },
  "type": "module"
}
